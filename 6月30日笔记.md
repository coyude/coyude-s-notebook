# 6月30日笔记

## 寻找数组的中心索引 （1991）https://leetcode.cn/leetbook/read/array-and-string/yf47s/

给你一个整数数组 nums ，请计算数组的 中心下标 。

数组 中心下标 是数组的一个下标，其左侧所有元素相加的和等于右侧所有元素相加的和。

如果中心下标位于数组最左端，那么左侧数之和视为 0 ，因为在下标的左侧不存在元素。这一点对于中心下标位于数组最右端同样适用。

如果数组有多个中心下标，应该返回 最靠近左边 的那一个。如果数组不存在中心下标，返回 -1 。

**示例 1**：

输入：nums = [1, 7, 3, 6, 5, 6]
输出：3
解释：
中心下标是 3 。
左侧数之和 sum = nums[0] + nums[1] + nums[2] = 1 + 7 + 3 = 11 ，
右侧数之和 sum = nums[4] + nums[5] = 5 + 6 = 11 ，二者相等。

**示例 2**：

输入：nums = [1, 2, 3]
输出：-1
解释：
数组中不存在满足此条件的中心下标。
**示例 3**：

输入：nums = [2, 1, -1]
输出：0
解释：
中心下标是 0 。
左侧数之和 sum = 0 ，（下标 0 左侧不存在元素），
右侧数之和 sum = nums[1] + nums[2] = 1 + -1 = 0 。


提示：

1 <= nums.length <= 104
-1000 <= nums[i] <= 1000

我的思路：

分类讨论，最左端，最右端和中间端。代码：

```c
int pivotIndex(int* nums, int numsSize)
{
    int n=numsSize - 1;
    if (n == 0)//特殊情况，当且只有一个元素时
        return 0;
    for (int i = 0; i <= n; i++)
    {
        if (i == 0)//中心下标在最左端时
        {
            int sumr = 0;
            for (int k = 1; k <= n; k++)
            {
                sumr += nums[k];
                if (sumr == 0 && k == n)
                {
                    answer = 0;
                    return answer;
                }
            }
        }
        if (i == n)//中心下标在最右端时
        {
            int suml = 0;
            for (int k = n-1; k >= 0; k--)
            {
                suml += nums[k];
                if (suml == 0 && k == 0)
                {
                    answer = n;
                    return answer;
                }
            }
        }
        if (i > 0 && i < n)//中心下标在中间端时
        {
            int sumL = 0;
            int sumR = 0;
            for (int k = i - 1; k >= 0; k--)//计算左侧和
            {
                sumL += nums[k];
            }
            for (int j = i + 1; j <= n; j++)//计算右侧和
            {
                sumR += nums[j];
            }
            if (sumL == sumR)
            {
                answer = i;
                return answer;
            }
        }
    }
    return -1;//均无果
}
```

执行用时**1116ms**，在所有C提交中击败了5%的用户（也就是说我这算法烂得很-.-）

而**官方给出的标准答案如下**：

```c
int pivotIndex(int* nums, int numsSize) {
    int total = 0;
    for (int i = 0; i < numsSize; ++i) {
        total += nums[i];
    }
    int sum = 0;
    for (int i = 0; i < numsSize; ++i) {
        if (2 * sum + nums[i] == total) {
            return i;
        }
        sum += nums[i];
    }
    return -1;
}
```

原理:**设数组内所有元素的和为total，但遍历到第i个元素时，其左侧元素和为sum，则其右侧元素和为total-sum-nums[i]，故左右侧相等时则有这个式子sum=total-sum-nums[i]，即2*sum+nums[i]=total**

这个公式对我来说，就好像高斯求解1-100之间的和一样，简单而遥不可及😅（#流汗黄豆）






